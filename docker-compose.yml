services:
  racadm:
    image: racadm:latest
    container_name: racadm
    build:
      context: .
      dockerfile: Dockerfile
      args:
        IDRAC_TARBALL: "${IDRAC_TARBALL}"
    #entrypoint: ["racadm", "--nocertwarn", "-r", "${IDRAC_IP}", "-u", "${IDRAC_USER}", "-p", "${IDRAC_PASS}"]
    network_mode: host
    restart: "no"

# Usage: docker compose run --rm racadm <command>
# Example: docker compose run --rm racadm getsysinfo
